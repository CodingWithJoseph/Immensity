[pytest]
pythonpath = src .
testpaths = tests
addopts = -m "not gpu" -p no:cacheprovider
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    gpu: requires a GPU and/or large model download (skipped by default in CI)
